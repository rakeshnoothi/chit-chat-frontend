import { Stack } from "@mui/material";

const ChatSection = () => {
    return (
        <Stack flexGrow={1} border={1}>
            ChatSection
        </Stack>
    );
};
export default ChatSection;
